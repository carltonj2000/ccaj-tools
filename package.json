{
  "name": "ccaj-tools",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "run": "bun run ./index.ts",
    "compile": "bun build --compile --target=bun-linux-x64 ./index.ts --outfile ccaj-tools"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  }
}
